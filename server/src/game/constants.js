const config = require('../config');

module.exports = {
  TICK_RATE: config.TICK_RATE,
  TICK_INTERVAL: config.TICK_INTERVAL,
  TANK_SPEED: config.TANK_SPEED,
  TANK_ROTATION_SPEED: config.TANK_ROTATION_SPEED,
  TANK_SIZE: config.TANK_SIZE,
  TANK_HALF: config.TANK_SIZE / 2,
  TANK_BARREL_LENGTH: config.TANK_BARREL_LENGTH,
  BULLET_SPEED: config.BULLET_SPEED,
  BULLET_RADIUS: config.BULLET_RADIUS,
  MAX_BOUNCES: config.MAX_BOUNCES,
  MAX_SIMULTANEOUS_BULLETS: config.MAX_SIMULTANEOUS_BULLETS,
  SHOOT_COOLDOWN: config.SHOOT_COOLDOWN,
  BULLET_SUB_STEPS: config.BULLET_SUB_STEPS,
  RESPAWN_DELAY: config.RESPAWN_DELAY,
  ROUND_DURATION: config.ROUND_DURATION,
  SCORE_TO_WIN: config.SCORE_TO_WIN,
  RESPAWN_SHIELD_TIME: config.RESPAWN_SHIELD_TIME,
  CELL_SIZE: config.CELL_SIZE,
  MAZE_ROWS: config.MAZE_ROWS,
  MAZE_COLS: config.MAZE_COLS,
  WALL_THICKNESS: config.WALL_THICKNESS,
  CANVAS_WIDTH: config.CANVAS_WIDTH,
  CANVAS_HEIGHT: config.CANVAS_HEIGHT,
};
